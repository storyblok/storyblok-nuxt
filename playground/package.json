{
  "private": true,
  "scripts": {
    "demo": "nuxt dev",
    "demo-https": "nuxt dev --https --ssl-cert localhost.pem --ssl-key localhost-key.pem",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "^3.0.0",
    "@storyblok/nuxt": "^0.0.1"
  }
}
